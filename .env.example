# Variáveis para docker-compose.prod.yml
# Copie para .env e preencha os valores

POSTGRES_USER=postgres
POSTGRES_PASSWORD=SUA_SENHA_FORTE_AQUI
POSTGRES_DB=bom_jesus

JWT_SECRET=GERE_COM_openssl_rand_base64_48

# URL que o frontend usa para chamar a API (deve ser acessível pelo browser)
VITE_API_URL=http://localhost:3000

# Origens permitidas no CORS (separadas por vírgula). Vazio = permite todas.
# CORS_ORIGIN=https://app.seudominio.com.br,https://www.seudominio.com.br
